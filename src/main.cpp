#include <iostream>

using namespace std;

int main(int argc, char *arvg[]) {
  string word;
  while (getline(cin, word)) {
    cout << word << endl;
  }

  cout << "Y U NO LIKE ECHO? " << endl;

  return 0;
}
