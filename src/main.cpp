#include <iostream>

using namespace std;

int main(int argc, char *arvg[]) {
  string line;
  while (getline(cin, line)) {
    cout << line << endl;
  }

  cout << "Y U NO LIKE ECHO? " << endl;

  return 0;
}
